import React from 'react';

const PreTransitionView = () => (
  <>
    <h1>Pre-Transition View</h1>
    <p>This is a view of the UI prior to a transition.</p>
  </>
);

export default PreTransitionView;
